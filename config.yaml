system:
  demo: false

data:
  # Only used by bot (spread) - needs to run `run_server_info` first
  market: ws://localhost:8000

support-resistance:
  period: 120
  multiplier: 1.2

risk-management:
  take-profit-ampl: 2.2
  stop-loss-ampl: 1
